import React from 'react';
import { LivewireContext } from 'types';
type Props = {
    component: LivewireContext;
};
export default function LivewireMemo(props: Props): React.JSX.Element | null;
export {};
